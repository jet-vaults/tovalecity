# tovalecity

## Status

| | |
|---|---|
| **Domain** | `https://tovalecity.co.il` |
| **Pages URL** | `https://tovalecity.pages.dev` |
| **Storage mode** | `Standard` (`standard`) |
| **Storage account** | `jetvaults` |
| **Public storage** | `https://jetvaults.blob.core.windows.net/tovalecity/` |
| **Private storage** | `https://jetvaults.blob.core.windows.net/tovalecity-private/` |
| **Public container** | `tovalecity` |
| **Private container** | `tovalecity-private` |
| **Activated** | No |

## Nameservers

Set these at your domain registrar:

```
gordon.ns.cloudflare.com
sureena.ns.cloudflare.com
```

## Development

Edit files in `wwwroot/` and push to `main` - Cloudflare Pages auto-deploys.

Only the `wwwroot/` directory is served. Everything else stays in the repo.
